1. 准备好相应的gro文件和table_list.dat
2. GenpOMFF.py中写入体系包含的分子名字和个数
3. 运行GenpOMFF.py即可

1. Prepare the corresponding .gro file and table_list.dat.
2. In GenpOMFF.py, specify the names and counts of the molecules included in the system.
3. Run GenpOMFF.py.